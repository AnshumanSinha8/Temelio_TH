function sendEmail(to, subject, body) {
    // Replace console.log with a real email service call if wanted
    console.log(`[MOCK SEND] To: ${to} | Subject: ${subject} | Body: ${body}`);
  }
  
  module.exports = {
    sendEmail
  };
  